import { MyProfile } from "@/types/profile";

/**
 * TEMP: replace with DB query later
 */
export async function getMyProfile(): Promise<MyProfile | null> {
    return null; // user not onboarded yet
}